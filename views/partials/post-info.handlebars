<article class="challenges priya-hero-form">
  <div class="title">
    <h2>{{title}}</h2>
  </div>
  <div class="meta">
   {{content_txt}} 
   by {{user.username}} on {{format_date createdAt}}
    
    {{comments.length}} {{format_plural "comment" comments.length}}
  </div>
</article>